function AuthLoadingScreen() {
  return (
    <div style={{
      width: "100%",
      height: "100vh",
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      fontSize: "1.2rem",
      color: "#444"
    }}>
      Verificando sessão...
    </div>
  );
}

export default AuthLoadingScreen;
