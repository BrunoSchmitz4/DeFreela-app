🧩 Estado Atual do Front-End — Projeto DeFreela
🎯 Visão Geral

O projeto DeFreela é uma aplicação React estruturada com base em uma arquitetura modular e escalável.
O foco atual é a simulação do front-end completo antes da integração real com a API (que virá de um back-end em Spring Boot).
O ecossistema é voltado ao desenvolvimento limpo, componentizado e mobile-first, com foco em reutilização e clareza de responsabilidades.

⚙️ Tecnologias e Ecossistema

React JS (com Hooks e JSX) — base da aplicação.

React Router DOM — controle de rotas e navegação.

Axios — abstraído em /services/api.js para futuras chamadas HTTP.

CSS Modules — utilizado para estilização isolada e responsiva.

Arquitetura Component-Based (Atomic-like) — separação clara entre UI Components, Feature Components e Pages.

Mock Data — usado em /mocks/ para simular retorno da API.

Context API — base preparada em /context/authContext.jsx para autenticação global.

Estrutura do projeto/
src/
├── assets/                 # Recursos estáticos (imagens, ícones, etc)
├── components/             # Componentes reutilizáveis e genéricos
│   ├── CardList/           # Exibição genérica de listas de cards
│   ├── ComponentContainer/ # Containers reutilizáveis para layout
│   ├── CreateProjectModal/ # Modal para criação de novos projetos
│   ├── DropdownButton/     # Botão dropdown para ações contextuais
│   ├── FreelancerCards/    # Card individual de freelancer
│   ├── Header/             # Cabeçalho global da aplicação
│   ├── PrivateRoute/       # Wrapper para proteger rotas privadas (futuro)
│   ├── ProjectCard/        # Card individual de projeto
│   ├── RouterLink/         # Link personalizado para navegação entre páginas (SPA)
│   ├── Tabs/               # Link personalizado para navegação entre conteúdos da página
│   └── ui/                 # Componentes de interface base (UI Primitives)
│       ├── Button/
│       ├── Input/
│       └── Modal/
│
├── context/
│   ├── authContext         # Contexto de autenticação (login, logout, usuário)
│   ├── freelancerContext   # Contexto de freelancers
│   └── project             # Contexto de projetos
│
├── hooks/                  # Custom Hooks reutilizáveis (ainda em desenvolvimento)
│   ├── useContracts        # Hook de contratos (deprecatedo)
│   ├── useFreelancers      # Hook de freelancers
│   └── useProjects         # Hook de projetos
│
├── mocks/
│   ├── freelancers         # Mock de freelancers
│   └── projects            # Mock de projetos
│
├── pages/                  # Páginas principais (camada View)
│   ├── Auth/               # Login e autenticação
│   ├── BasePage/           # Layout base contendo Header e Outlet
│   ├── Freelancer/         # Página de freelancers
│       ├── MyJobs            # SubPágina de Trabalhos do usuário
│       └── SearchFreelancer  # Subpágina de busca de freelancers
│   ├── Home/               # Página inicial com overview e botões de navegação
│   ├── NotFound/           # Página 404
│   ├── Profile/            # Página de perfil do usuário logado e de exibição de perfil de outros usuários
│   └── Projects/           # Página de Projetos
│       ├── MyProjects        # SubPágina de Projetos do usuário
│       ├── ProjectDetails    # SubPágina de detalhes do projeto
│       └── SearchProjects    # Subpágina de busca de projetos
├── services/
│   ├── api                 # Instância Axios configurada
│   ├── authService         # Servieec de autenticação (login, logout)
│   ├── contractService     # Service de contratos
│   ├── freelancerService   # Service de freelancers
│   └── projectService      # Service de projetos
│
├── store/                  # (Reservado para uso futuro com Redux ou Zustand)
│
├── styles/                 # Estilos globais e temas
│
├── utils/                  # Funções utilitárias (helpers)
│
├── index.css               # Estilos globais iniciais
├── index.js                # Entry point da aplicação React
└── routes.js               # Definição das rotas da aplicação

🧭 Fluxo de Rotas

As rotas são controladas via react-router-dom e seguem um layout-base compartilhado (BasePage) que contém o cabeçalho principal.

        <BrowserRouter>
            <Routes>
                {/* Rota de Layout base */}
                <Route path="/" element={ <BasePage /> }>
                    {/* Rotas públicas */}
                    <Route index element={ <Home /> } />

                    {/* Rotas privadas (Quando tivermos a API para fazer o auth do usuário logado, irei encapsular essas tags marotas*/}
                    <Route path="/Profile" element={ <Profile /> } />
                    <Route path="/profile/:id" element={<Profile />} />

                    <Route path="/freelancers" element={ <Freelancer /> } >
                        <Route path="/freelancers/searchFreelancers" element={ <SearchFreelancer /> }/>
                        <Route path="/freelancers/myJobs" element={ <MyJobs />  }/>
                    </Route>
                    <Route path="/projects" element={ <Projects /> } >
                        <Route path="/projects/searchProjects" element={ <SearchProjects />  }/>
                        <Route path="/projects/myProjects" element={ <MyProjects />  }/>
                        <Route path="/projects/:id" element={<ProjectDetails />} />
                    </Route>

                    {/* Rota Fallback */}
                    <Route path="*" element={ <NotFound /> } />
                </Route>
                    <Route path="/login" element={ <Login /> } />
            </Routes>
        </BrowserRouter>

Página	Hook / Context	Casos de Uso
/projects/myProjects	useProjects()	UC01–UC05
/projects/searchProjects	useProjects()	UC08
/freelancers/searchFreelancers	useFreelancers()	UC06–UC07
/freelancers/myJobs	useFreelancers()	UC09–UC11
/profile/:id	useAuth + useContexts	exibe visão geral completa

Esse fluxo garante uma navegação fluida, com o Header fixo e renderização condicional do conteúdo conforme o Outlet do React Router.
🧠 Estado Atual

✅ Front-end pronto para integração real com a API
✅ Componentes reutilizáveis e consistentes
✅ Design responsivo e minimalista (mobile-first)
✅ Simulação funcional com mocks e estados locais
🔜 Próximos passos:

Integrar API real do Spring Boot com Axios.

Implementar autenticação real (login/logout + PrivateRoute).

Conectar dados dinâmicos (projetos, freelancers, perfil).

Refinar UI com paleta final e padronização de temas globais.

🧩 Observações Gerais

A aplicação segue o princípio Container + Presentational Components, mantendo lógica separada da UI.

A estrutura atual já considera a escalabilidade futura (por exemplo, integração com Redux ou Zustand).

O objetivo principal é oferecer uma base sólida para a camada de consumo de dados do back-end, preservando modularidade e clareza.